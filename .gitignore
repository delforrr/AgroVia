# --- DEPENDENCIAS (Aplica a Client y Server) ---
node_modules
package-lock.json # Opcional: A veces se commitea, a veces no. Yo recomiendo SÍ commitearlo para consistencia.
yarn.lock
pnpm-lock.yaml

# --- VARIABLES DE ENTORNO (CRÍTICO) ---
# Nunca subas esto. Aquí están tus claves de Supabase y DB.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env
*.env.*

# --- BUILD & FRONTEND (Client) ---
dist/
dist-ssr/
build/
*.local

# --- BACKEND & LOGS (Server) ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock

# --- SISTEMA OPERATIVO & EDITOR (Basura) ---
.DS_Store
Thumbs.db
.idea
.vscode
*.swp
*.swo

# --- TESTING ---
coverage/